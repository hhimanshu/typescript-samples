/*
1.
a. number
b. string
c. pineapples
d. boolean[]
e. {type: string}
f. (number | boolean)[]
g. number[]
h. null (correct: any)
 */
let h = null

/*
2.
a. because type of i is a type literal 3
b. because j is inferred as number[]. Adding a string value is not allowed.
c. never does not have any value defined
d. unknown type has to be refined further before it can performed operation. See Type Refinement
 */

// to match answers see https://github.com/bcherny/programming-typescript-answers/blob/master/src/ch03/exercises.ts